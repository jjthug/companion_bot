# Companion for Seniors

## Run App Locally

Start the application using Docker Compose:

```bash
docker compose up -d
```

## Stop and Remove the Application

To stop and remove the application, including associated volumes:

```bash
docker compose down -v
```
